Install 2.5 version of SFML:
Ubuntu :    sudo apt install libsfml-dev

Compile and install Pinguin library:
    Exécuter 'sudo make install' à la racine du repo

Uninstall Pinguin library:
    Exécuter 'sudo make uninstall' à la racine du repo

Compile example :
    g++ -o {appname} main.cpp -lPinguin -lsfml-graphics -lsfml-window -lsfml-system -pthread
